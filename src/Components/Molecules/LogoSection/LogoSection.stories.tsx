import * as React from 'react';
import { LogoSection } from './LogoSection';

export default {
  title: 'Molecules/LogoSection',
  component: LogoSection,
};

const Template : any = () => <LogoSection />;

export const Logo = Template.bind({});
Logo.args = {};
